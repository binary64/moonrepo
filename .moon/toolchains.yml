# https://moonrepo.dev/docs/config/toolchain
$schema: "./cache/schemas/toolchains.json"

# In v2, shared JS settings moved from node: to javascript:
javascript:
  packageManager: "bun"
  syncProjectWorkspaceDependencies: true

# bun: block required when javascript.packageManager = "bun"
bun: {}

# Keep node: section to activate the Node.js toolchain
node: {}

typescript:
  createMissingConfig: true
  routeOutDirToCache: false
  syncProjectReferences: true

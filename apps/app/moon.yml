# https://moonrepo.dev/docs/config/workspace
$schema: "../../.moon/cache/schemas/workspace.json"

language: 'node'
type: 'application'

tags:
  - next

tasks:
  lint:
    command: "biome check ."
  type-check:
    command: "tsc --noEmit"

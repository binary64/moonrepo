type: application
language: node

tasks:
  copy-data:
    command: 'cp $MOON_WORKSPACE_ROOT/apps/pawpicks-uk/src/_data/products.json ./products.json'
    inputs:
      - '$workspaceRoot/apps/pawpicks-uk/src/_data/products.json'
    outputs:
      - 'products.json'

  build:
    command: 'tsc --noEmit'
    inputs:
      - 'src/**/*.ts'
      - 'tsconfig.json'

  type-check:
    command: 'tsc --noEmit'
    inputs:
      - 'src/**/*.ts'
      - 'tsconfig.json'

  lint:
    command: 'biome check .'
    inputs:
      - 'src/**/*.ts'

  build-image:
    command: 'docker build -t pawpicks-stock-checker .'
    deps:
      - 'copy-data'
      - 'build'
    inputs:
      - 'Dockerfile'
      - 'src/index.ts'
      - 'package.json'
      - 'package-lock.json'
      - 'tsconfig.json'
      - 'products.json'

typescript:
  syncProjectReferences: false

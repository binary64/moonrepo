# GENERATED — do not edit. Edit devices.yaml and run generate-devices.py
substitutions:
  name: triang1
  friendly_name: "Triangulation Node 1"

esphome:
  name: ${name}
  friendly_name: ${friendly_name}

esp32:
  board: esp32dev
  minimum_chip_revision: "3.1"
  framework:
    type: arduino

packages:
  common: !include packages/triang-common.yaml

substitutions:
  name: triang3
  friendly_name: "Triangulation Node 3"

esphome:
  name: ${name}
  friendly_name: ${friendly_name}

esp32:
  board: esp32dev
  minimum_chip_revision: "3.1"
  framework:
    type: arduino

packages:
  common: !include packages/triang-common.yaml

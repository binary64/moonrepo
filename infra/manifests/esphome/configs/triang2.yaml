# GENERATED — do not edit. Edit devices.yaml and run generate-devices.py
substitutions:
  name: triang2
  friendly_name: Triangulation Node 2

esphome:
  name: ${name}
  friendly_name: ${friendly_name}

esp32:
  board: esp32dev
  framework:
    type: arduino

packages:
  common: !include packages/triang-common.yaml

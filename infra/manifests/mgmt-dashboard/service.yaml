apiVersion: v1
kind: Service
metadata:
  name: mgmt-dashboard
  namespace: mgmt-dashboard
  labels:
    app.kubernetes.io/name: mgmt-dashboard
    app.kubernetes.io/instance: mgmt-dashboard
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: mgmt-dashboard
    app.kubernetes.io/instance: mgmt-dashboard

apiVersion: v1
kind: Service
metadata:
  name: lan-bridge
  namespace: lan-bridge
  labels:
    app: lan-bridge
spec:
  type: ClusterIP
  selector:
    app: lan-bridge
  ports:
    - port: 3080
      targetPort: 3080
      protocol: TCP
      name: http

apiVersion: v1
kind: Service
metadata:
  name: zeroclaw
  namespace: zeroclaw
  labels:
    app.kubernetes.io/name: zeroclaw
    app.kubernetes.io/instance: zeroclaw
spec:
  type: ClusterIP
  ports:
    - port: 42617
      targetPort: gateway
      protocol: TCP
      name: gateway
  selector:
    app.kubernetes.io/name: zeroclaw
    app.kubernetes.io/instance: zeroclaw

apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: radio-web
  namespace: radio-web
spec:
  parentRefs:
    - name: gateway
      namespace: istio-system
  hostnames:
    - "radio.brandwhisper.cloud"
  rules:
    - backendRefs:
        - name: radio-web
          port: 3004

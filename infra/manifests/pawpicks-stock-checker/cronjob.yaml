apiVersion: batch/v1
kind: CronJob
metadata:
  name: pawpicks-stock-checker
  namespace: pawpicks-stock-checker
  annotations:
    moonrepo.dev/skip-local: "true"
  labels:
    app: pawpicks-stock-checker
spec:
  # Run every hour at 5 minutes past (gives Vercel build time to pick it up)
  schedule: "5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 3
  jobTemplate:
    spec:
      backoffLimit: 2
      activeDeadlineSeconds: 600
      template:
        metadata:
          labels:
            app: pawpicks-stock-checker
        spec:
          restartPolicy: OnFailure
          automountServiceAccountToken: false
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            runAsGroup: 1000
            fsGroup: 1000
            seccompProfile:
              type: RuntimeDefault
          containers:
            - name: stock-checker
              # Tag is replaced by the docker.yml GHA workflow on every master push.
              # Placeholder SHA — docker.yml pins this to the real digest on first master push.
              image: ghcr.io/binary64/pawpicks-stock-checker:0000000
              imagePullPolicy: IfNotPresent
              securityContext:
                allowPrivilegeEscalation: false
                readOnlyRootFilesystem: false
                capabilities:
                  drop:
                    - ALL
              env:
                - name: HASURA_ENDPOINT
                  value: http://hasura.hasura.svc.cluster.local:8080
                - name: HASURA_ADMIN_SECRET
                  valueFrom:
                    secretKeyRef:
                      name: hasura-admin
                      key: HASURA_GRAPHQL_ADMIN_SECRET
              resources:
                requests:
                  cpu: 100m
                  memory: 128Mi
                limits:
                  cpu: 500m
                  memory: 256Mi

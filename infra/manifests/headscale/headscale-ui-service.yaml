apiVersion: v1
kind: Service
metadata:
  name: headscale-ui
  namespace: headscale
  labels:
    app.kubernetes.io/name: headscale-ui
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: headscale-ui
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP

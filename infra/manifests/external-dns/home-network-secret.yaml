apiVersion: v1
kind: Secret
metadata:
  name: home-network
  namespace: external-dns
type: Opaque
stringData:
  # Home public IP — updated automatically by wan-ip-monitor via ConfigMap.
  # This secret is consumed by external-dns as --default-targets.
  #
  # DO NOT EDIT MANUALLY — instead update the rke2-config ConfigMap
  # (infra/manifests/rke2-config/configmap.yaml) and run the WAN IP monitor.
  public-ip: "83.105.77.69"

apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: icecast-stream
  namespace: radio-dj
spec:
  parentRefs:
    - name: gateway
      namespace: istio-system
  hostnames:
    - "stream.brandwhisper.cloud"
  rules:
    - backendRefs:
        - name: icecast
          port: 8100

apiVersion: v1
kind: Service
metadata:
  name: arthur-radio
  namespace: arthur-radio
  labels:
    app: arthur-radio
spec:
  type: ClusterIP
  selector:
    app: arthur-radio
  ports:
    - port: 8100
      targetPort: 8100
      protocol: TCP

apiVersion: v1
kind: Service
metadata:
  name: tts-server
  namespace: tts-server
  labels:
    app: tts-server
spec:
  type: NodePort
  selector:
    app: tts-server
  ports:
    - port: 3090
      targetPort: 3090
      nodePort: 30090
      protocol: TCP

# PLACEHOLDER — generate this with kubeseal before deploying:
#
#   kubectl create secret generic github-webhook-secret \
#     --namespace github-webhook \
#     --from-literal=webhook-secret='YOUR_SECRET_HERE' \
#     --dry-run=client -o yaml | \
#   kubeseal --controller-namespace kube-system -o yaml > infra/manifests/github-webhook/sealed-secret.yaml
#
# The webhook secret should match what you configure in GitHub repo settings.
# Generate one with: openssl rand -hex 32

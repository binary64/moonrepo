apiVersion: v1
kind: Namespace
metadata:
  name: github-webhook
  annotations:
    moonrepo.dev/skip-local: "true"
    # Bootstrap resource — must exist before workloads (wave 2)
    argocd.argoproj.io/sync-wave: "-2"

apiVersion: v1
kind: Service
metadata:
  name: ptt-server
  namespace: ptt-server
  labels:
    app: ptt-server
spec:
  type: NodePort
  selector:
    app: ptt-server
  ports:
    - port: 9876
      targetPort: 9876
      nodePort: 30987
      protocol: TCP

apiVersion: v1
kind: Namespace
metadata:
  name: litellm
  # Ambient mesh disabled: ztunnel HBONE tunnel between NUC (gateway) and
  # Jupiter (litellm pod) times out on every request (0/6774 success).
  # LiteLLM is a proxy itself — mesh adds no value here.

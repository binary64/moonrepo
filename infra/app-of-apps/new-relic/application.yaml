# https://artifacthub.io/packages/helm/newrelic/nri-bundle
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: newrelic
  namespace: argocd
  annotations:
    # Requires licenseKeySecret which isn't available in CI
    moonrepo.dev/skip-local: "true"
spec:
  project: default

  source:
    repoURL: https://helm-charts.newrelic.com
    chart: nri-bundle
    targetRevision: 6.0.32
    helm:
      releaseName: newrelic
      values: |
        global:
          cluster: binary64
          customSecretName: newrelic-secret
          customSecretLicenseKey: licenseKey
          lowDataMode: true

        # --------------------------------
        # Infrastructure Agent (DaemonSet)
        # nri-bundle V3 requires per-component resources
        # --------------------------------
        newrelic-infrastructure:
          enabled: true
          kubelet:
            resources:
              requests:
                cpu: 50m
                memory: 96Mi
              limits:
                cpu: 200m
                memory: 256Mi
          ksm:
            resources:
              requests:
                cpu: 50m
                memory: 96Mi
              limits:
                cpu: 200m
                memory: 256Mi
          controlPlane:
            resources:
              requests:
                cpu: 50m
                memory: 96Mi
              limits:
                cpu: 200m
                memory: 256Mi

        # --------------------------------
        # Kube State Metrics
        # --------------------------------
        kube-state-metrics:
          enabled: true

        # --------------------------------
        # Logging (EU endpoint)
        # --------------------------------
        newrelic-logging:
          enabled: true
          endpoint: https://log-api.eu.newrelic.com/log/v1
          resources:
            requests:
              cpu: 30m
              memory: 96Mi
            limits:
              cpu: 200m
              memory: 256Mi

        # --------------------------------
        # Metadata Injection
        # --------------------------------
        nri-metadata-injection:
          enabled: true

        # --------------------------------
        # Kubernetes Events
        # --------------------------------
        nri-kube-events:
          enabled: true

        # --------------------------------
        # Prometheus Scraping
        # --------------------------------
        nri-prometheus:
          enabled: true

        # --------------------------------
        # Pixie (explicitly disabled)
        # --------------------------------
        pixie-chart:
          enabled: false

  destination:
    server: https://kubernetes.default.svc
    namespace: newrelic

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true

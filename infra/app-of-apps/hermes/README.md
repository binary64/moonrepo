# Hermes

Namespace stub for the Hermes agent. The agent currently runs host-side on the
`contabo-portsmouth` node; this namespace exists so SealedSecrets and a future
Deployment manifest can land without a second PR.

See `hermes/README.md` at the repo root for the host-side bootstrap flow.

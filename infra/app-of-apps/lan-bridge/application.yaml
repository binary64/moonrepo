apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: lan-bridge
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/binary64/moonrepo.git
    path: infra/manifests/lan-bridge
    targetRevision: HEAD

  destination:
    server: https://kubernetes.default.svc
    namespace: lan-bridge

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: matter-hub
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/binary64/moonrepo.git
    path: infra/manifests/matter-hub
    targetRevision: HEAD

  destination:
    server: https://kubernetes.default.svc
    namespace: matter-hub

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

# Hermes env file template.
# The real file lives at /home/hermes/.hermes/.env (0600, gitignored).
# bootstrap-env.sh generates it from AWS Secrets Manager.
# Do NOT put real values here.

# Home Assistant long-lived access token
HASS_URL=http://home-assistant.home-assistant.svc.cluster.local:8123
HASS_TOKEN=

# Add more hermes-* secrets below as they are introduced.
